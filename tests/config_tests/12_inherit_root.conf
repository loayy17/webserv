http {
    server {
        listen localhost:8080;
        root /var/www;
        location / {
            index index.html;
        }
        location /images {
            autoindex on;
        }
    }
}
