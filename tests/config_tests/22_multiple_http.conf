http {
    server {
        listen localhost:8080;
        root /var/www;
        location / {
            index index.html;
        }
    }
}
http {
    server {
        listen localhost:9090;
        root /var/www;
        location / {
            index index.html;
        }
    }
}
