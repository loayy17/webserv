http {
    server {
        listen localhost:8080;
        server_name localhost;
        server_name example.com;
        root /var/www;
        location / {
            index index.html;
        }
    }
}
