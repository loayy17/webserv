http {
    client_max_body_size 1M;
    client_max_body_size 2M;
    server {
        listen localhost:8080;
        root /var/www;
        location / {
            index index.html;
        }
    }
}
